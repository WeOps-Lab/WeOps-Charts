apiVersion: apps/v1
kind: Deployment
metadata:
  name: weops-agent
  labels:
    app: weops-agent
spec:
  selector:
    matchLabels:
      app: weops-agent
  template:
    metadata:
      labels:
        app: weops-agent
    spec:
      serviceAccount: {{ .Release.Name }}-kube-state-metrics
      containers:
      - name: grafana-agent
        env:
        - AGENT_MODE: flow
        {{- if .Values.resources }}
        resources:
{{ toYaml .Values.resources | indent 10 }}
        {{- end}}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        args:
        - "run"
        - "/etc/agent/flow.yaml"
        ports:
          - containerPort: 12345
        volumeMounts:
        - name: agent-config
          mountPath: /etc/agent/flow.yaml
          subPath: ./flow.yml
      volumes:
      - name: agent-config
        configMap:
          name: weops-grafan-agent-config
          items:
          - key: flow.yml
            path: ./flow.yml